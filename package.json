{
  "name": "jwalin-shah-github-io",
  "private": true,
  "scripts": {
    "build": "esbuild app-entry.jsx --bundle --format=iife --outfile=dist/app.js",
    "smoke": "npm run build && python3 scripts/validate_publication.py && python3 scripts/smoke_static_build.py",
    "validate": "npm run smoke"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
